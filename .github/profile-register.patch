--- a/app/main.py
+++ b/app/main.py
@@ -661,12 +661,14 @@
 from app.routers.connections import router as connections_router
 from app.routers.device_create import router as device_create_router
 from app.routers.racks import router as racks_router
+from app.routers.profile import router as profile_router
 from app.routers.search import router as search_router
 from app.routers.system import router as system_router
 
 app.include_router(device_create_router)
 app.include_router(connections_router)
 app.include_router(racks_router)
+app.include_router(profile_router)
 app.include_router(search_router)
 app.include_router(system_router)
 app.include_router(admin_router)
--- a/app/core/auth.py
+++ b/app/core/auth.py
@@ -322,6 +322,9 @@
         }:
             return "dashboard.view"
 
+        if path.startswith("/profile"):
+            return ""
+
         if path == "/search" or path.startswith("/api/search"):
             return "search.view"
 
--- a/app/templates/base.html
+++ b/app/templates/base.html
@@ -128,6 +128,15 @@
                 </a>
                 {% endif %}
 
+                <a
+                    class="nav-item
+                    {% if current_page == 'profile' %}active{% endif %}"
+                    href="/profile"
+                >
+                    <span class="nav-icon">◎</span>
+                    <span>Mi perfil</span>
+                </a>
+
                 {% if "users.manage" in permissions or "roles.manage" in permissions or "audit.view" in permissions or "system.view" in permissions %}
                 <div class="nav-section-label">Administración</div>
                 {% endif %}
@@ -224,7 +233,7 @@
                         {% if write_enabled | default(false) %}Escritura controlada{% else %}Solo lectura{% endif %}
                     </span>
 
-                    <div class="current-user">
+                    <a class="current-user" href="/profile" aria-label="Abrir mi perfil">
                         <span class="current-user-avatar">
                             {{ (current_user or "U")[0] | upper }}
                         </span>
@@ -233,7 +242,7 @@
                             <strong>{{ current_user }}</strong>
                             <span>{{ role_label }}</span>
                         </div>
-                    </div>
+                    </a>
 
                     <form
                         action="/logout"
